name: Run presubmit-test

on:
  pull_request_review:
    branches: [main]
    types: [submitted]

jobs:

  test:
    if: github.event.review.state == 'approved'

    runs-on: ubuntu-latest

    steps:
      - run: echo "hello world"
